{
  "name": "search_assignments_by_title",
  "description": "通过关键词搜索课程作业，支持模糊匹配标题。",
  "parameters": {
    "type": "object",
    "properties": {
      "course": {
        "type": "string",
        "description": "课程名称，可模糊匹配"
      },
      "query": {
        "type": "string",
        "description": "用于搜索的关键词，模糊匹配作业标题"
      }
    },
    "required": ["course", "query"]
  }
}